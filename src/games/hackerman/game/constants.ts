export const COUNTDOWN_LABELS = ["3", "2", "1", "YA!"];
export const COUNTDOWN_STEP = 0.8; // segundos por etiqueta
export const MAX_DT = 0.1; // clamp del delta time para evitar saltos grandes

/** Mejor tiempo local (centisegundos), por juego. */
export const BEST_KEY = "hackerman_best_centis";

/** Cantidad de niveles (subjuegos) que hay que superar para terminar. */
export const LEVEL_COUNT = 3;
